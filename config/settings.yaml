# Configuration utilisateur du bot.
# Ã‰dite ces valeurs selon tes prÃ©fÃ©rences.

# --- Comportement gÃ©nÃ©ral ---

# Par dÃ©faut, le bot tourne en dry-run (aucun appel d'Ã©criture vers ManyReach).
# Passe Ã  false UNIQUEMENT quand tu es confiant.
default_dry_run: true

# Seuil de confidence en-dessous duquel on ne fait JAMAIS d'auto-send.
# Le draft est gÃ©nÃ©rÃ©, mais reste en review.
min_autosend_confidence: 0.0

# Modèles Claude (modifiables ici quand de meilleurs sortent — Claude proposera
# des upgrades qualité régulièrement). Classifier = rapide/éco, drafter = qualité.
models:
  classifier: "claude-haiku-4-5-20251001"
  drafter: "claude-sonnet-4-6"

# Comportement sur not_interested_polite.
# Rudy a confirmÃ© : il rÃ©pond TOUJOURS (politesse + referral-ask + valeur), pas de silence.
# Mettre Ã  true uniquement si tu veux override ce comportement par dÃ©faut.
silent_on_not_interested: false

# Nombre max de replies traitÃ©s par run (utile pour les premiers tests).
# null = pas de limite.
limit_per_run: 20

# Si null, on traite toutes les campagnes. Sinon liste d'IDs.
campaign_filter: null
#  - 11572

# --- IdentitÃ© ---
# La voice, les signatures par entitÃ©, et les liens backup sont dÃ©finis
# dans training_examples.md (sections "Voice globale" + "EntitÃ©s"). Le drafter
# parse ce doc directement â€” pas besoin de dupliquer ici.

# --- Calendrier (phase 2 â€” Google Calendar) ---
# Pour l'instant, le bot utilise un pattern textuel pour les crÃ©neaux.
# En phase 2, on connectera Google Calendar.

calendar:
  enabled: true
  timezone: "Europe/Paris"
  # Horaires bookables de Rudy : 9h30-11h et 12h-17h30
  # (jamais avant 9h30 ni aprÃ¨s 17h30 ; pause course Ã  pied 11h-12h exclue)
  working_hours:
    monday:    [["09:30", "11:00"], ["12:00", "17:30"]]
    tuesday:   [["09:30", "11:00"], ["12:00", "17:30"]]
    wednesday: [["09:30", "11:00"], ["12:00", "17:30"]]
    thursday:  [["09:30", "11:00"], ["12:00", "17:30"]]
    friday:    [["09:30", "11:00"], ["12:00", "17:30"]]
  meeting_duration_minutes: 30
  buffer_minutes: 15
  days_ahead: 5
  hold_days: 5
  # Jamais de crÃ©neau Ã  moins de 3h du moment prÃ©sent
  min_lead_hours: 3
  # Si on discute aprÃ¨s 16h, ne pas proposer le lendemain avant cette heure
  # (Rudy ne verrait pas un RDV tÃ´t le lendemain matin)
  late_cutoff_hour: 16
  next_day_min_time_if_late: "10:30"
  # Lien Zoom de secours si le prospect veut absolument une visio
  zoom_link: "https://us02web.zoom.us/s/9136208131"

# --- Cadence de relance (follow-up) ---
# Combien de fois et quand le bot relance selon l'Ã©tat du lead.
followups:
  # IntÃ©ressÃ© / demande d'info SANS RDV calÃ© â†’ on relance pour obtenir le RDV
  interested_awaiting_booking:
    bump_after_days: [2, 5]   # relance #1 Ã  J+2, #2 Ã  J+5
    then: "nurture"           # aprÃ¨s, on arrÃªte et on archive en nurture long terme
  # Objection timing avec date mentionnÃ©e â†’ recontact Ã  la date
  objection_timing:
    recontact: "date_mentioned_or_3_months"
  # Pas intÃ©ressÃ© / dÃ©jÃ  Ã©quipÃ© / mauvaise personne â†’ 1 seule rÃ©ponse, pas de relance
  one_shot_no_bump: ["not_interested_polite", "objection_already_have_solution", "wrong_person_redirect"]
  # RDV confirmÃ© â†’ 1 accusÃ© "c'est notÃ©" puis STOP total
  meeting_confirmed: "ack_then_stop"

# --- Envoi (anti-grillage + montÃ©e en charge) ---
sending:
  # N'envoyer que pendant ces heures (sinon le draft attend le prochain run en heures ouvrÃ©es).
  # Ã‰vite les rÃ©ponses Ã  3h du matin = signature de robot.
  allowed_hours: [7, 22]              # envoi autorisÃ© de 9h Ã  19h
  # Rudy ENVOIE en cold uniquement la semaine, mais il veut que les REPONSES du
  # bot partent vite, y compris samedi/dimanche. D'ou 7j/7 ici.
  allowed_weekdays: [0, 1, 2, 3, 4, 5, 6]   # lundi=0 ... dimanche=6 (week-end inclus)
  # Ne JAMAIS rÃ©pondre instantanÃ©ment : attendre que le reply ait "vieilli" un peu.
  # (un humain ne rÃ©pond pas en 3 secondes). RÃ©glÃ© en minutes, lÃ©gÃ¨rement randomisÃ©.
  min_reply_age_minutes: 12
  max_reply_age_jitter_minutes: 25    # Ã¢ge effectif = entre min et min+jitter (alÃ©atoire par prospect)
  # Petit dÃ©lai alÃ©atoire entre 2 envois d'un mÃªme run (court, compatible Vercel).
  inter_send_jitter_seconds: [3, 12]
  # Plafond d'envois par exÃ©cution (sÃ©curitÃ© montÃ©e en charge).
  max_sends_per_run: 25
  # PROTECTION BACKLOG : ne traiter QUE les replies reÃ§us aprÃ¨s cette date.
  # Mets la date/heure de mise en route pour ignorer tout ton historique dÃ©jÃ  gÃ©rÃ©.
  # Format ISO. null = pas de filtre (dÃ©conseillÃ© en prod).
  process_replies_since: "2026-05-22T00:00:00+02:00"

# --- Logs ---

logs:
  dir: "logs"
  format: "jsonl"  # jsonl = un JSON par ligne, facile Ã  grep
